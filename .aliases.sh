# Kubectx
alias kx='kubectx'
alias kn='kubens'

# Virtualenv
alias activate='source ./.venv/bin/activate'

# Bat (better cat)
alias cat='bat --paging=never --color=always --style=plain --theme=Nord'

# ---- Eza (better ls) -----
alias ls="eza --color=always --long --git --icons=always --all --group-directories-first"
alias ll="eza --color=always --long --git --icons=always --all --group-directories-first"

# ---- zoxide (better cd) -----
alias cd='z'

# Reload config
alias reload='source ~/.zshrc'
