# Kubectx
alias kx='kubectx'
alias kn='kubens'

# Virtualenv
alias activate='source ./.venv/bin/activate'

# Bat (better cat)
alias cat='bat'

# ---- Eza (better ls) -----
alias ls="eza --color=always --long --git --icons=always --all --group-directories-first"
alias ll="eza --color=always --long --git --icons=always --all --group-directories-first"

# Reload config
alias reload='source ~/.zshrc'